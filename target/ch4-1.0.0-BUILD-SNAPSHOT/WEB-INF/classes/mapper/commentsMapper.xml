<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myportfolio.web.repository.CommentsMapper">
    <select id="selectAll" parameterType="int" resultType="Comments">
        select id,comment,update_date,board_id,user_id,ifnull(pcid,id) from comments
        where board_id = #{board_id}
        order by pcid asc, cid asc
    </select>

    <select id="select" resultType="Comments" parameterType="Integer">
        select * from comments
        where id = #{id}
    </select>

    <delete id="deleteAll" parameterType="int">
        delete from comments
        where board_id = #{board_id}
    </delete>

    <select id="count" parameterType="int" resultType="int">
        select count(*) from comments
        where board_id = #{board_id}
    </select>

    <delete id="delete" parameterType="map">
        delete from comments where id = #{id} and user_id = #{user_id}
    </delete>

    <insert id="insert" parameterType="Comments">
        insert into comments
            (id, comment, created_date, updated_date, post_id, user_id, pcid)
        values
            (null, #{comment}, now(), now(), #{post_id}, #{user_id}, #{pcid})
    </insert>

    <update id="update" parameterType="Comments">
        update comments
        set comment = #{comment}
            ,updated_date = now()
        where id = #{id} and user_id = #{user_id}
    </update>

    <select id="findLastId">
        select id from comments
        order by id desc
        limit 0,1
    </select>

</mapper>