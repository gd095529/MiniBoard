<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myportfolio.web.repository.UserMapper">
    <select id="selectOne" resultType="User" parameterType="Integer">
        select * from user
        where id = #{id}
    </select>

    <select id="selectAll" resultType="User">
        select * from user
    </select>

    <insert id="insert" parameterType="User">
        insert into user(username,password,nickname,email,created_date,updated_date)
        values (#{username}, #{password}, #{nickname}, #{email}, now(), now())
    </insert>

    <delete id="delete" parameterType="Integer">
        delete from user
        where id = #{id}
    </delete>

    <delete id="deleteAll" >
        delete from user
    </delete>

    <update id="update" parameterType="User">
        update user set password = #{password}, username = #{username}, nickname = #{nickname}, updated_date = now()
        where id = #{id}
    </update>

    <select id="count" resultType="int">
        select count(*) from user
    </select>
</mapper>