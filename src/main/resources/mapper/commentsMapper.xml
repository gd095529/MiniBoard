<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myportfolio.web.repository.CommentsMapper">
    <select id="selectAll" parameterType="int" resultType="Comments">
        select * from comments
        where id = #{id}
        order by id asc
    </select>

    <select id="selectOne" resultType="Comments" parameterType="Integer">
        select * from comments
        where id = #{id}
    </select>

    <delete id="deleteAll" parameterType="int">
        delete from comments
        where id = #{id}
    </delete>

    <select id="count" parameterType="int" resultType="int">
        select count(*) from comments
        where id = #{id}
    </select>

    <delete id="delete" parameterType="map">
        delete from comments where id = #{id} and user_id = #{user_id}
    </delete>

    <insert id="insert" parameterType="Comments">
        insert into comments
            (id, comment, created_date, updated_date, post_id, user_id)
        values
            (${id}, #{comment}, now(), now(), #{post_id}, #{user_id})
    </insert>

    <update id="update" parameterType="Comments">
        update comments
        set comment = #{comment}
            ,updated_date = now()
        where id = #{id} and user_id = #{user_id}
    </update>
</mapper>